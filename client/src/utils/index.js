// 工具类导入导出入口文件

import {http} from './http'

import {
    setToken,
    getToken,
    removeToken,
    setGmail,
    getGmail,
    removeGmail,
    setUName,
    getUName,
    removeUName,
    setId,
    getId,
    removeId
} from './ls'

import {history} from './history'

export {
    http,
    setToken,
    getToken,
    removeToken,
    setGmail,
    getGmail,
    removeGmail,
    setUName,
    getUName,
    removeUName,
    setId,
    getId,
    removeId,
    history
}

